# 開発用構成。`docker compose run --rm app <コマンド>` の形で使う。
services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: dev # 配布用（dist）ステージと分けるため明示指定
    working_dir: /app
    volumes:
      - .:/app
