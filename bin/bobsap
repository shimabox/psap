#!/usr/bin/env php
<?php

// bobsap CLI のエントリポイント。
// いまはバージョン表示ができるだけの土台。コマンドは後続フェーズで追加する。

declare(strict_types=1);

require __DIR__ . '/../vendor/autoload.php';

use Symfony\Component\Console\Application;

$application = new Application('bobsap', '0.1.0');
$application->run();
